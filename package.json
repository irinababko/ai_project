{
  "name": "proximity-ranking-site",
  "version": "1.0.0",
  "private": true,
  "description": "Proximity Ranking — static marketing site (HTML + SCSS).",
  "scripts": {
    "build": "sass scss/main.scss css/main.css --style=compressed --no-source-map",
    "dev": "sass --watch scss/main.scss:css/main.css --style=expanded",
    "start": "npx serve -l 3000 ."
  },
  "devDependencies": {
    "sass": "^1.77.8"
  }
}
